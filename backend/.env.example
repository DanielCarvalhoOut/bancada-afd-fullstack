# Banco PostgreSQL (a porta pedida no projeto)
DB_HOST=localhost
DB_PORT=54327
DB_USER=bancada
DB_PASSWORD=bancada
DB_NAME=bancada_afd

# API
PORT=3000
# origem permitida no CORS (front Angular)
CORS_ORIGIN=http://localhost:4200
